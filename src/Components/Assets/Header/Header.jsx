import s from "./Header.module.css"
const Header = () => { 
    return (
        <header className={s.header}>
            <p className={s.text}>Notes</p>
        </header>
    )
}

export default Header